#ifndef USER_INPUT_H
#define USER_INPUT_H

namespace UserInput
{
    bool isValidCommand(char c);
    void ignoreLine();
    char getCharacter();
    char getCommand();
} // namespace UserInput

#endif
